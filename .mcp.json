{
  "mcpServers": {
    "_comment_": "Shared with the team, keep it versioned. NEVER write tokens here - use $VARIABLE, read from the environment. Personal servers go through `claude mcp add --scope local`. PSsimTool is a desktop 3D application - browser/DB MCP servers have no business here, which is why they are gone.",

    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": {
        "Authorization": "Bearer $GITHUB_TOKEN"
      }
    }
  }
}
